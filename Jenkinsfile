pipeline {
    agent any

    stages {
        stage('build') {
            steps {
                echo 'buildd'
            }
        }
      stage('test') {
            steps {
                echo 'test'
            }
        }
      stage('deploy') {
            steps {
                echo 'deploy'
            }
        }
    }
}
