pipeline {
    agent any

    stages {
        stage('build') {
            steps {
                echo 'build'
            }
        }
      stage('test') {
            steps {
                echo 'testtt'
            }
        }
      stage('deploy') {
            steps {
                echo 'deploy'
            }
        }
    }
}
