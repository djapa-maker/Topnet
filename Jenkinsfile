pipeline {
    agent any

    stages {
        stage('build') {
            steps {
                echo 'build'
            }
        }
      stage('test') {
            steps {
                echo 'testt'
            }
        }
      stage('deploy') {
            steps {
                echo 'deploy'
            }
        }
    }
}
